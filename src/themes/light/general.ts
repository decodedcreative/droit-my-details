import colors from "./colors";
import spacing from "./spacing";

const general = {
  borderRadius: "8px",
  borderWidth: "1px",
  focusWidth: "2px",
  focusBoxShadow: `0 0 0 2px ${colors.background}, 0 0 0 4px ${colors.interactive}`,
  gridMaxWidth: "1128px",
  gridGutter: spacing.lg,
};

export default general;
